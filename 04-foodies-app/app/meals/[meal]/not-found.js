export default function NotFound() {
    return (
        <main className="not-found">
            <h1>Meal Not found</h1>
            <p> Unfortunately, we couldn't find the requested resource or meal data.</p>
        </main>
    );
}